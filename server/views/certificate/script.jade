script(src='/bower_components/jquery/dist/jquery.min.js')
script.
  $('.cert-name').css({
    position : 'absolute',
    left : '72%',
    top : '46%',
    'margin-left': -$('.cert-name').outerWidth() / 2,
    'margin-top': -$('.cert-name').outerHeight() / 2
  });

  $('.cert-link').css({
    position : 'absolute',
    left : '50%',
    top : '93%',
    'margin-left': -$('.cert-link').outerWidth() / 2,
    'margin-top': -$('.cert-link').outerHeight() / 2
  });
